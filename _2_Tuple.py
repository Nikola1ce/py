# 元组（Tuple）
# 元组是一种不可变的序列类型，一旦创建，不能修改。
# 特点：
# 不可变：元组中的元素不能被修改。
# 元素可以是任意类型。
# 支持索引和切片操作。
# 常用操作：
# Python
# 复制
# 创建元组
my_tuple = ('1', 2, 3, 4, 5)

# 访问元素
print(my_tuple[0])  # 输出 1

# 切片
print(my_tuple[1:3])  # 输出 (2, 3)

# 元组解包
a, b, c, d, e = my_tuple
print(a, b, c, d, e)  # 输出 1 2 3 4 5